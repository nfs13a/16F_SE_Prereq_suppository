We will be able to read data from the CSV and store it in a database.
We will be able to read data from the database and report which students from that database may take Dr. Clements' courses.
However, we will not be running test cases on the CSV. Instead, we will be testing on smaller CSV's to prove that it works for the larger, real version, because running on the real CSV takes too long.

We will need to modify current data structure uses to make this work.